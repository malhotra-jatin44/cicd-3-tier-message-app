apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  namespace: messaging-app
  name: auditor-role

rules:

- apiGroups: ["*"]

  resources: ["*"]

  verbs:
    - get
    - list
    - watch